import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  submitBtn: {
    marginTop: 43,
    paddingHorizontal: 32,
    paddingVertical: 12,

    backgroundColor: "#FF6C00",
    borderWidth: 1,
    borderColor: "#FF6C00",
    borderRadius: 100,
  },
  submitBtnText: {
    fontSize: 16,
    color: "#fff",
    textAlign: "center",
  },
});

export const { submitBtn, submitBtnText } = styles;
