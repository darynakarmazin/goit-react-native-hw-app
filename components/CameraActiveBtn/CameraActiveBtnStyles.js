import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  addImageBtn: {
    justifyContent: "center",
    alignItems: "center",
    padding: 18,

    borderRadius: 30,
    backgroundColor: "#fff",
    zIndex: 1001,
  },
});

export const { addImageBtn } = styles;
